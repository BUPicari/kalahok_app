import 'package:flutter/material.dart';

class UnknownQuestionWidget extends StatelessWidget {
  const UnknownQuestionWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // fix this ui later
    return const Center(child: Text('Unknown question type'));
  }
}
